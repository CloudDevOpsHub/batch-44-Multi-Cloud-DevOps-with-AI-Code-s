

sudo -i

# 1. download the script
#
#   $ curl -fsSL https://get.docker.com -o install-docker.sh
#
# 2. verify the script's content
#
#   $ cat install-docker.sh
#
# 3. run the script with --dry-run to verify the steps it executes
#
#   $ sh install-docker.sh --dry-run
#
# 4. run the script either as root, or using sudo to perform the installation.
#
#   $ sudo sh install-docker.sh

in short 

sudo -i
curl -fsSL https://get.docker.com -o install-docker.sh
sudo sh install-docker.sh
docker -v



manual 

apt-get update
apt-get install nginx -y

Mohit Sharma 8:22



Run is from OS level 
CMD is from Application level CLI output 


Dockerfile

FROM ubuntu
MAINTAINER clouddevopshub@gmail.com
RUN apt-get update
RUN apt-get install nginx -y
CMD [“echo”,”Image created”]


my history 

 1  curl -fsSL https://get.docker.com -o install-docker.sh
    2  sudo sh install-docker.sh
    3  docker -v
    4  ls
    5  vi Dockerfile
    6  cat Dockerfile 
    7  ls
    8  dicker images
    9  docker images
   10  docker build -t swarnandu44 .
   11  docker images
   12  docker tag 59aafac36b78 vikas4cloud/swarnandu44:latest
   13  docker images
   14  docker login
   15  docker images
   16  docker push vikas4cloud/swarnandu44:latest
   17  docker pull shalinisaini24/shaliniimage
   18  docker images
   19  docker pull devopsprajyot/prajyotaplpine:tagname
   20  docker pull devopsprajyot/prajyotaplpine
   21  docker images
   22  history
